import React from 'react'

const HomePage = () => {
    return (
        <div className='flex flex-col'>
            {/* navBar*/}
            // ? sign up button
            // ? login button
            // ? become a mentor
            // ?  find a mentor link
            // ? logo (typography)

            {/* middle */}
            //* SearchBar
            //*  general description

            {/* footer */}
        // ? Github repo
        // ? linked pages (team members)
        // ? copyright
        </div>
    )
}

export default HomePage
